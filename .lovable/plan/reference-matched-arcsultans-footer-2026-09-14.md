# Reference-matched ARCSultans footer

## What will change
- Rebuild only the page footer to closely follow the attached pixel-art composition.
- Keep the current ARCSultans wordmark, mint line, destinations, and social URLs unchanged.
- Add the ARC NETWORK identity block, centered navigation, top-right collection line, and rightmost dynasty statement.
- Use the existing X, Telegram, and OpenSea artwork already stored in the project.
- Make the footer part of the page layout so it never covers the main content.
- Stack and resize the same sections cleanly on tablet and mobile without changing their hierarchy.

## Technical details
- Replace the current fixed two-column footer markup with a responsive grid matching the reference proportions.
- Add semantic footer surface and border tokens to the existing design system.
- Keep square, sharp-edged social controls with accessible labels and visible focus states.
- Verify desktop and mobile appearance, all links, content clearance, and the existing three-state flow.
